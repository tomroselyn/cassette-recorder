import React from 'react';
import './Boombox.css';

const WavDisplay = (props) => {
    return (
        <div className="top-panel-wav-display">
            wav display
        </div>
    );
}

export default WavDisplay;